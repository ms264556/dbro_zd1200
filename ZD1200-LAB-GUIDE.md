# ZD1200 Lab Guide — stock boot under QEMU (no initramfs, no handoff)

This guide covers the QEMU lab for the Ruckus ZoneDirector 1200 (10.5.1.0.282)
brought up in this directory.  The guest boots **exactly like the physical
appliance**: QEMU loads the patched kernel, the kernel mounts `root=/dev/hda2`
directly from the CompactFlash image (no initramfs), and the stock vendor
userspace runs unmodified.  A root shell on the guest sees a real ZD1200.

Everything below is the *outcome* of debugging this setup — the "Development
hints" section at the end collects the non-obvious traps that cost the most
time.  Read it before changing anything.

---

## 1. What was added/changed in this repo

| file | role |
|---|---|
| `patch-kernel.py` | applies the QEMU-required byte patches to the stock kernel and splices it back into a bootable `bzImage` (the old KVM-only gdb flow is gone) |
| `make-synthetic-cf.py` | builds the synthetic CompactFlash: MBR + root image on hda1/hda2/hda3, and a **real ReiserFS** data partition on hda4 |
| `write-boarddata.py` | writes the ZD board-data records (serial, MACs, model) at the exact sectors a real appliance uses, plus the `0x55AA` partition-magic sector the vendor kernel requires |
| `run-zd1200-lab.sh` | one-shot launcher: validates/builds `image/`, patched kernel, VM disk; then execs QEMU |
| `run-zd1200-qemu.sh` | QEMU wrapper; the NIC is `model=igb` so the stock igb2 driver binds |

The guest is stock: the vendor kernel (patched only where QEMU hardware
differs), the vendor root filesystem, the vendor init scripts, the vendor
web/emfd/controller stack.  No initramfs, no boot handoff, no dropbear-2222,
no seeded accounts.

All QEMU networking uses `NETWORK_MODE=user` (SLIRP) with host forwards bound
to `127.0.0.1` — a loopback-only adapter.  No Docker is required.

## 2. Prerequisites

- x86_64 (or aarch64 — see hints) Linux host, `qemu-system-i386` + `qemu-img`
  (Debian/Ubuntu: `apt install qemu-system-x86 reiserfsprogs`), `python3`,
  `curl`, ~6 GB free disk.
- `/dev/kvm` optional: with KVM the same patched kernel boots (the patches are
  static bytes; no gdb flow is involved either way).
- `prepare-vendor-image.sh` must have been run once (see below) to populate
  `image/` from your decrypted `zd1200_10.5.1.0.282.ap_10.5.1.0.282.img.tgz`
  (SHA-256 `64dfbf4d…`).

## 3. One-time preparation

```sh
./prepare-vendor-image.sh /abs/path/to/zd1200_10.5.1.0.282.ap_10.5.1.0.282.img.tgz
```

This produces `image/bzImage`, `image/vmlinux`, `image/rootfs.ext2`
(**gzip-compressed — see hint #1**), `image/restoreinitramfs.gz` and the
payload tarball.

## 4. Launch

```sh
./run-zd1200-lab.sh              # builds anything missing, then starts QEMU
```

Optional flags: `--reset-disk` (factory reset), `--rebuild-kernel`,
`--wait [SECS]`.  The script prints the URLs before exec'ing QEMU; the guest
console is attached (Ctrl-A X to quit QEMU).

Under TCG expect **~2–4 minutes** from launch to the login page / wizard.

## 5. Verify

```sh
# web (factory wizard until configured):
curl -kIs https://127.0.0.1:38443/admin10/login.jsp     # expect 302 wizard.jsp
```

Complete the factory wizard to create the admin account (this is stock
behavior — nothing is pre-seeded).  After the wizard finishes and the
container/VM is restarted once, the stock administrative SSH (Dropbear on
port 22, host key generated by the vendor `sys_init`) is available to that
account.

## 6. How the boot works

1. QEMU loads the patched kernel; the kernel mounts `root=/dev/hda2` from the
   synthetic CF directly (no initramfs, exactly like the physical appliance).
2. The vendor `v54bsp` driver reads the board data (serial, MACs, model) from
   the CF at the real sectors; the stock `sys_init` mounts hda4 at `/writable`
   with its stock `-o …,nolog` command (hda4 is real ReiserFS, so the option
   is valid and `mount` output matches real hardware).
3. The stock `S40network` script loads `igb2.ko` for the COB7402 platform;
   QEMU's `igb` NIC model is an Intel 82576 (PCI 0x10C9) which igb2.ko
   supports, so eth0/br0 come up with the board-data MAC.
4. The vendor S-scripts and `controller`/`ac_init` bring up the ZD stack and
   web server.  `rc.pre_ac_init` generates certs and SSH host keys on first
   boot because the board data provides a serial number.
5. The board-data retry path and the physical watchdog/restart/halt paths are
   the only kernel-level changes (`patch-kernel.py`) — QEMU hardware
   accommodations, invisible to a root shell.

## 7. Development hints (non-obvious traps)

1. **aarch64 hosts work as-is.**  The old initramfs/handoff build needed
   `as --32` / `ld -m elf_i386` (i686 binutils) to assemble `pivot-exec.S`;
   that flow is gone, and `prepare-vendor-image.sh` / `patch-kernel.py` /
   the launch scripts are pure bash + Python + tar, so no binutils shim is
   required.

2. **`image/rootfs.ext2` is gzip, not ext2.**  The vendor archive stores
   `rootfs.i386.ext2.director1200.img` compressed; `prepare-vendor-image.sh`
   copies it verbatim, so the synthetic CF partitions are seeded with gzip
   bytes and `sys_init`'s mount of hda2 fails.  Decompress it first.  Note the
   ext2 superblock magic `0xEF53` lives at byte **1080** (`s_magic` =
   superblock + `0x38`), not 1024.  `run-zd1200-lab.sh` decompresses
   automatically.

3. **TCG cannot run the old gdb patch, so the kernel is patched
   statically.**  The original gdb patch flow used `hbreak`, which
   QEMU's TCG backend rejects.  `patch-kernel.py` applies the same edits as
   static byte writes to the kernel ELF.  **Critical layout detail:** in this
   vendor bzImage the gzip member does *not* run to EOF — a second-stage boot
   loader is linked *after* the member, and the head code jumps to a baked
   offset of it.  Only the member region may be replaced; its end is found via
   the gzip trailer (`crc32(payload) + ISIZE` of the decompressed ELF), and
   the recompressed stream must not exceed the original member length.

4. **Board data must be a unicast MAC.**  `write-boarddata.py` writes MAC1
   into the CF board-data records; the kernel's v54bsp driver forces that
   address onto the NIC at probe time.  A multicast MAC (e.g. `01:01:01:01:01:02`)
   makes igb2 fail the probe (`Invalid MAC Address`, error -5) and the guest
   has no network.  The default is the unicast `00:0c:e6:12:00:01`
   (MAC2 = MAC1 + 1).

5. **The vendor kernel reads the partition table from the board-data sector.**
   `fs/partitions/msdos.c` in this kernel reads `ZD_PART_SECTOR` (3927001)
   first: if that sector carries the `0x55AA` magic, it is used as the
   partition table and sector 0 is ignored.  `write-boarddata.py` therefore
   writes an MBR-style table there.  **Boot flags must be 0x00 or 0x80** — the
   vendor `msdos_partition()` rejects any entry whose boot indicator is
   neither, printing "hda: unknown partition table" (the real CF uses 0x01,
   but this kernel refuses it).

6. **hda4 must be real ReiserFS.**  The stock `sys_init` mounts the data
   partition with `-o …,nolog`, a ReiserFS-only option; ext2 fails with
   EINVAL and `/writable` never mounts.  `make-synthetic-cf.py` formats hda4
   with `mkreiserfs` (requires reiserfsprogs on the build host), so the stock
   mount works unmodified and `mount` shows `reiserfs` like a real ZD1200.

7. **No initramfs, no handoff.**  The physical appliance boots kernel →
   rootfs directly; this repo now does the same.  The old initramfs/handoff
   flow (`boot-initrd-handoff` / `make-boot-initrd.sh` /
   `make-runtime-initrd.sh`) has been removed from the tree.

8. **Persistence and factory reset.**  All VM writes (controller state, host
   keys, wizard-created accounts) live in the qcow2 overlay `zd1200-vm.qcow2`
   over `synthetic-cf.img`; deleting both (`--reset-disk`) returns the VM to
   the factory wizard.  In a dockerized deployment, that overlay is what
   should live on a named volume so `/writable` persists.

9. **Running QEMU from scripts/CI.**  QEMU keeps the terminal busy, so
   automated launchers hang waiting on the pipe; detach with
   `setsid env … ./run-zd1200-qemu.sh </dev/null >log 2>&1 &`.

10. **KVM is fine too.**  With `/dev/kvm` present, `ACCEL=kvm
    ./run-zd1200-lab.sh` boots the same patched kernel.

11. **After the wizard: restart once.**  The configured system generates its
    persistent Dropbear host key and starts the stock administrative SSH
    (port 22) on the next boot, exactly like a real appliance.

## 8. Troubleshooting cheat sheet

| symptom | likely cause / fix |
|---|---|
| `System halted.` right after `nar5520_wdt_init` | unpatched kernel — run `patch-kernel.py` |
| guest EIP sleds through zeros after boot | loader tail was overwritten — rebuild with `patch-kernel.py` |
| no web on 38443; log shows `Invalid MAC Address` | board-data MAC is multicast — use a unicast MAC (hint #4) |
| `hda: unknown partition table` | ZD_PART_SECTOR boot flag is not 0x00/0x80, or the sector is missing — re-run `write-boarddata.py` (hint #5) |
| `/writable` mount fails with EINVAL | hda4 is ext2, not ReiserFS — rebuild with `make-synthetic-cf.py` (hint #6) |
| web never becomes ready after ~4 min | QEMU port 38080/38443 still held by a stale qemu process — kill it (hint #9) |
| wizard hangs on key generation | low-entropy factory phase; give the guest time (first-boot keygen) |
